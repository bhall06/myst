const { baka } = require("./baka");
const { help } = require("./help");
const { ip } = require("./ip");
const { ping } = require("./ping");
const { start } = require("./start");
const { fractal } = require("./fractal");
const { say } = require("./say");
const { react } = require("./react");

module.exports = {
  baka,
  help,
  ip,
  ping,
  start,
  fractal,
  say,
  react,
};
